import React from 'react';
import SimpleButton from '../atoms/button';

const LabelButton = (label: string) => {
  return <SimpleButton label={label} onClick={() => {}} />;
};

export default LabelButton;
